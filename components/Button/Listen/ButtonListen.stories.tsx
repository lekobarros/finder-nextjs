import { Meta, StoryObj } from '@storybook/react';
import Button from './index';

export default {
  title: 'Components/Button/Listen',
  component: Button
} as Meta;

export const Default : StoryObj = {

}